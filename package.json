{
  "name": "driver-signal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply driver_signal_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply driver_signal_db --remote",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9",
    "wrangler": "^3.114.15"
  },
  "dependencies": {
    "hono": "^4.6.14"
  }
}
